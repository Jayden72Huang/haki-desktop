# HackerTrip 桌面助手 · 使用说明书

版本对应分支 `feat/race-mode-cli-assist`（2026-08-22）。

本文讲**怎么用**。想看这次改了什么、踩了哪些坑，见 [ITERATION.md](./ITERATION.md)。

---

## 一、这是什么

黑客松选手的桌面伴侣。常驻屏幕一个悬浮胶囊条，做三件事：

- **平时**：盯着你本机跑的所有 coding agent（Claude Code / Codex / Gemini…），显示每个会话在干什么、在哪个分支、改了多少
- **比赛时**：倒计时 + 赛程节点提醒，告诉你此刻该做什么
- **交付时**：把项目拖进窗口，本地 CLI 提炼成参赛作品，推到 HackerTrip 小程序

所有 AI 能力都调**你本机已登录的 claude CLI**，不需要额外配 API Key，对话内容不出本机。

---

## 二、启动

```bash
cd app
npm install
npm run tauri dev
```

> **重要**：窗口是 **660×380 无边框透明悬浮窗，不进 Dock 任务栏**。启动后请在屏幕上找那个置顶的胶囊条，不要去 Dock 里找图标。

需要本机已安装 `claude` 命令（AI 功能依赖它，缺了只影响 AI 相关按钮，其他照常）。

---

## 三、界面结构

### 收起态：胶囊条

拖住任意空白处可移动位置。右侧两个徽标随模式变化：

| 模式 | 左侧徽标 | 右侧状态 |
|---|---|---|
| 日常 | `今日 286.4M tokens` | `3 个 agent 运行中` |
| 比赛 | `距提交 12:34:56` | `进行中` / **`最后冲刺`**（剩 <2 小时转红） / `已结束` |

点胶囊条展开面板，点左上角 `⌄` 收起。

### 展开态：面板

顶部右侧是「**日常 / 比赛**」模式切换，下面一排是入口：

- `赛事 ↗` `我的 ↗` `指南 ↗` — 用浏览器打开 hackertrip.space 对应页面
- `设置` — 在面板内切换

---

## 四、日常模式

### 1. 今日 Agent 用量

当天所有 Claude Code 会话的 token 总量与会话数。数据来自本地 `~/.claude/projects/` 日志，按本地时区的今天零点起算（与 `git --since=midnight` 口径一致）。

### 2. 运行中的 Coding Agent

核心区域。每个正在跑的 agent 进程一条，双行：

```
●  [logo]  给白皮书加宇航员行走动画            12 改动 +340/-28  ›
           hackertrip-miniprogram  ⑂ feat/discover-category-works
```

**主行**：状态点 · agent logo · **这个会话在干什么** · 改动量
**次行**：所在文件夹 · 当前分支

点任意一条可展开，看完整任务描述、完整路径、分支、状态。

**状态点含义**：

| 点 | 含义 |
|---|---|
| 🔵 蓝色（呼吸） | 运行中 |
| 🟠 橙色（快速呼吸） | **待确认** — agent 停在工具调用上等你批准 |
| 🟡 黄色 | 已完成，你还没看过 |
| 🟢 绿色 | 已完成，已查看（展开过就算看过） |

**agent 用 logo 区分**，不显示 CLI 名字：Claude 是橙色星芒，Codex 是蓝色圆环，Gemini 是四角星，其他 agent 显示首字母方块。

**「改动」的口径是 git 未提交改动**——`git status` 数出来的文件数，加上 `git diff` 的行级增删，和旁边的分支字段是同一套语义。不是 agent 的编辑次数。

**任务概述从哪来**：
- Claude Code：会话日志里最后一条真实用户指令
- Codex：优先用 codex 自己给会话起的标题；正在跑的会话通常还没有标题，这时回退到首条用户指令

### 3. 今日开发数据

按仓库列出今天的 commit 数、最近一次 commit 的时间与信息、未推送数量。**超过 45 分钟没有 commit 会变成橙色警告图标**，防止长时间不落盘丢代码。

### 4. AI 每日总结

点「生成今日 AI 总结」，把当天的开发数据交给本地 claude CLI，输出四段：今日概览、今日亮点、值得沉淀成 skill 的重复流程、明日建议。

每晚 22:00 会发一条系统通知提醒你生成。

---

## 五、比赛模式

### 1. 开始一场比赛

切到「比赛」，填三样东西：

| 字段 | 必填 | 说明 |
|---|---|---|
| 比赛名称 | 是 | 会显示在胶囊条上 |
| 开始 / 提交截止 | 截止必填 | 倒计时按截止时间算 |
| **赛事文档** | 否 | **粘贴主办方的赛事文档或群公告** |

**强烈建议贴文档**。贴了之后：
- 自动排出带时间的赛程节点，而不是只有一个笼统的四阶段进度条
- 比赛期间可以随时问它赛事细节（Token 在哪领、作品交到哪）

点「开始比赛」后，本地 CLI 会解析文档并排赛程，通常十几秒。

### 2. 赛程节点

时间轴形式，每条是「时间 · 节点名 · 此刻要做什么」。已过去的节点会变灰划掉。

**红点是硬节点**——错过就无法挽回的（报名截止、提交截止、路演签到）。

**提醒规则**：
- 硬节点：**提前 30 分钟**推送系统通知
- 普通节点：到点推送

文档里写明的时间**原样保留不会被改**；文档没写的部分（比如「功能冻结」「演示视频定稿」）由 CLI 按经验推导补上。

右上角「重新规划」可以重新跑一次，改了比赛信息或想换个排法时用。

实际效果示例：

```
🔴 08-25 09:00  开幕式签到          云栖小镇A馆凭身份证签到入场
🔴 08-25 10:30  赞助商Token发放      B馆二楼凭队伍编号领API Key
   08-25 14:00  选题与仓库定版       锁定题目技术栈，建仓库并推首次提交
🔴 08-26 12:00  中期checkpoint      飞书群提交进度截图，不交视为弃赛
   08-26 22:00  功能冻结            停止加新功能，只修bug与写README
🔴 08-27 15:00  作品提交截止         提交仓库+演示视频
```

### 3. 交作品

赛程节点下方的「交作品」，点一下展开。

**第一步 · 提取**
把项目文件夹**直接拖进窗口**。本地 CLI 会读 README、package.json、最近 15 条 commit，提炼成作品卡片。拖进来时如果面板没展开到这块，会自动帮你打开。

**第二步 · 确认**
提取结果填进可编辑表单：作品名、一句话简介、仓库链接、demo 链接、技术栈。**都可以手改**——CLI 提取的不一定准，尤其 demo 链接，它拿不准时会留空而不是编一个。

要求：**作品名必填，仓库和 demo 至少要有一个 `http(s)` 链接**。这是小程序端的硬校验，本地会先拦一道，不合格不会白跑一趟网络。

**第三步 · 提交**
填入 6 位配对码后点「提交到 HackerTrip」。

> 配对码在**小程序「我的 → Skills 同步」**里生成。注意它和设置里那个「生成绑定二维码」的码**不是一回事**，后者是绑定网站账号用的。

提交成功后作品进入**待审核**状态，需要你在**小程序「我的 → 作品」**确认发布，才会公开到个人主页。

**第四步 · 发小红书（可选）**
提交成功后会多出「生成小红书文案」按钮。CLI 根据作品信息写标题+正文+话题标签，**自动复制到剪贴板**，点提示文字唤起小红书创作页，粘贴即可。

> 最后一步需要手动粘贴。小红书没有开放给第三方的发布接口，做不到真正一键发。

### 4. 问一下

比赛模式专有的输入框。把赛事文档和已排节点作为上下文，回答现场问题：

- 「赞助商 token 在哪领？」
- 「作品交到哪个链接？」
- 「路演要提前多久到？」

回车或点「问」都行。文档里没写的它会直说「文档里没写」再给建议，不会瞎编。

### 5. 其他

- **本场 Agent 用量** — 比赛期间的 token 消耗
- **最近 commit** — 超过 45 分钟没提交会告警
- **提交材料清单** — 四项默认清单（线上 demo 可访问 / README 补齐 / 演示视频 ≤3 分钟 / 完成平台提交），点击打勾
- **结束比赛** — 清空比赛配置并切回日常模式

---

## 六、设置

### 账号绑定与云端同步
GitHub 登录绑定账号，把近 30 天的统计数据同步到云端（用于个人主页的 AI 协作能力展示）。

### 小程序互绑
生成 6 位码 + 二维码，10 分钟有效。在小程序「我的 → 绑定网站账号 → 扫码」使用。**这个码不能用于交作品。**

### Agent 列表显示字段
五个开关，控制 session 条上显示什么：

`任务概述` · `所在文件夹` · `分支` · `改动` · `耗时 · tokens`

嫌信息密就关掉几个，设置本机保存。

### 高级
| 配置 | 用途 |
|---|---|
| API Key | 云端同步，也可由 GitHub 登录自动获取 |
| **pairSync 触发器地址** | **交作品必填** |
| **同步密钥 uploadToken** | **交作品必填** |

### 隐私
只上报统计特征与仓库名。**源码、对话内容、本地路径都不出本机。**

---

## 七、首次使用配置清单

| 想用的功能 | 需要配置 |
|---|---|
| 看 agent 列表 / 开发数据 | 无，装好就能用 |
| AI 总结 / 赛程规划 / 赛事问答 / 作品提取 | 本机装好 `claude` 命令并已登录 |
| 云端同步、个人主页 | 设置里 GitHub 登录 |
| **交作品** | 设置里填 pairSync 触发器地址 + uploadToken，另需小程序生成的 6 位配对码 |

pairSync 触发器地址在腾讯云该云函数的 HTTP 触发器页；uploadToken 在小程序配对时生成。对应 `hackertrip-cli` 的 `HACKERTRIP_SYNC_URL` / `HACKERTRIP_SYNC_TOKEN`。

---

## 八、常见问题

**启动了但找不到窗口**
无边框透明悬浮窗，不进 Dock。在屏幕上找置顶的胶囊条，别去 Dock 找图标。

**codex 那几行没有任务概述**
正在跑的 codex 会话如果既没有标题、首条用户指令又全是系统注入内容，会显示「暂无任务记录」。Claude Code 的会话不受影响。

**点了「开始比赛」但赛程一直转圈 / 报错**
本地 `claude` 命令不可用或未登录。终端里跑 `claude -p "hi"` 验证。

**拖文件夹进去没反应**
先点一下窗口让它获得焦点再拖。另外要拖**文件夹**，不是单个文件。

**提交作品报 INVALID_WORK**
作品名为空，或仓库/demo 都不是 `http(s)://` 开头。SSH 形式的 git 地址（`git@github.com:...`）会被自动转成 https，但如果项目压根没有 remote 就需要手填。

**磁盘满导致构建失败**
`cargo` 链接需要空间。`src-tauri/target` 通常占几个 G，`cargo clean` 可回收。

---

## 九、当前版本的限制

- **拖拽提取未经真机充分验证** — 逻辑完整，但真实拖放行为在 macOS 上偶尔需要窗口先获得焦点
- **小红书只到文案生成** — 见上文原因
- **「使用量」页面尚未上线** — 后端读取额度数据的能力已就绪（读 vibe-usage 落地的快照），前端待做
- **窗口不可缩放** — 660 宽固定，高度按内容自适应（上限 620）。内容多时面板内滚动
- **赛程规划依赖文档质量** — 主办方文档里没写具体时间的部分，CLI 只能按经验推导，重要节点建议自己核对一遍
